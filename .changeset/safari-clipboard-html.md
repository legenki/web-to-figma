---
"@figit/fig-kiwi": patch
"@figit/dom-to-figma": patch
---

Wrap Figma clipboard markers in `data-metadata` and `data-buffer` attributes so Safari/WebKit HTML clipboard sanitization preserves the payload.
